# Contributing

SideStage is community driven.

Keep device-specific behavior inside its device package. Preserve data you do not understand. Add tests for parser behavior and real hardware fixtures. Update documentation when behavior or architecture changes. Keep changes focused and reviewable.

Run `pnpm typecheck`, `pnpm test`, and `pnpm build` before submitting changes.
